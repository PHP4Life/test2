# Activity 4 - Modules

import random as rnd

minimum_value = input("Please enter the minimum value: ")
maximum_value = input("Please enter the maximum value: ")

rand_number = rnd.randrange(int(minimum_value), int(maximum_value))
